using System;
using UnityEngine;

public class Debugger : MonoBehaviour
{
    void Start()
    {
        /*
        Type t = ReflectionBase.TypeFromString("WalkState");
        dynamic state = Activator.CreateInstance(t);
        Debug.Log(state.namename);
        */
    }
}